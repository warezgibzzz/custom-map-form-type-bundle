{% block custom_map_widget %}
    <pre>{{ parameters|dump }}</pre>
    <div id="{{ id }}" class="{{ attr.class }}" style="width: {{ width }}px; height: {{ height }}px;">
        <img src="{{ params.image }}">
    </div>

    {% for child in form %}
        {% if child.vars.value is empty %}
            {{ form_row(child, {
                value: default[ child.vars.name ]
            }) }}
        {% else %}
            {{ form_row(child) }}
        {% endif %}
    {% endfor %}

    <script>
        var {{ name }}CustomMap = new CustomMapWidgetClass('{{ id }}', {{ parameters | json_encode | raw }});
    </script>
{% endblock %}
